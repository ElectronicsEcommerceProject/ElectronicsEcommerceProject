/**
 * System roles constants
 * Usage: 
 * import ROLES from './roles.js';
 * if (user.role === ROLES.ADMIN) { ... }
 */
const roles = {
    CUSTOMER: 'customer',
    RETAILER: 'retailer',
    ADMIN: 'admin'
  };

export const ROLES = roles;